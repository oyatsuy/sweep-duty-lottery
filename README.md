# 掃地工作抽籤

全班掃地工作分配抽籤工具（單一 HTML 檔，無需安裝、無需後端）。

- 每項工作預設 1 人，責任獨立、不能互推
- 涵蓋教室內外、團體輔導室、公共廁所等區域，可在網頁上直接編輯名單／工作清單
- 三種抽籤動畫：籤筒、轉盤、老虎機
- 可依「負責幹部」分組列印、匯出 CSV
- 可匯出／匯入設定 JSON，方便跨裝置或下學期重用
- 所有資料只存在瀏覽器本機（localStorage），不會上傳到任何伺服器

## 使用方式

直接開啟 GitHub Pages 網址，或下載 `index.html` 用瀏覽器打開即可使用。
